package com.l9rins.trademate.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Brand ──
val TradeMateGreen = Color(0xFF00E676)      // Vibrant mint green — primary action
val TradeMateGreenDark = Color(0xFF00C853)   // Darker green for pressed/active states
val TradeMateTeal = Color(0xFF00B0FF)         // Electric blue accent

// ── Neutrals ──
val TextPrimary = Color(0xFF0F1419)           // Near-black for headings
val TextSecondary = Color(0xFF6B7280)         // Slate grey for body/labels
val TextTertiary = Color(0xFF9CA3AF)          // Muted grey for hints

// ── Surfaces ──
val BackgroundLight = Color(0xFFF0F2F5)       // Cool light grey background
val SurfaceWhite = Color(0xFFFFFFFF)          // Card surface
val SurfaceElevated = Color(0xFFFAFBFC)       // Slightly raised surface
val CardBorder = Color(0xFFE5E7EB)            // Subtle border

// ── Dark Mode Surfaces ──
val DarkBackground = Color(0xFF0F1419)
val DarkSurface = Color(0xFF1A1F26)
val DarkSurfaceElevated = Color(0xFF252B35)
val DarkBorder = Color(0xFF2D3748)

// ── Status Colors ──
val StatusActive = Color(0xFF00B0FF)
val StatusPending = Color(0xFFFFB300)
val StatusPaid = Color(0xFF00E676)
val StatusOverdue = Color(0xFFFF5252)

// ── Trend Indicators ──
val TrendPositive = Color(0xFF00C853)
val TrendNegative = Color(0xFFFF5252)

// ── Gradients / Decorative ──
val GradientStart = Color(0xFF00E676)
val GradientEnd = Color(0xFF00B0FF)
val OrbGreen = Color(0xFF00E676)
val OrbBlue = Color(0xFF00B0FF)
val OrbPurple = Color(0xFF7C4DFF)